{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Copy to user folder",
      "type": "shell",
      "command": "${workspaceFolder}\\copy_to_user_folder.bat",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      },
      "problemMatcher": []
    }
  ]
}